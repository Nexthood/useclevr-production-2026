{
  "$schema": "https://backboard.railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm i -g corepack@latest 2>/dev/null || true; corepack enable 2>/dev/null || true; corepack prepare pnpm@11.5.0 --activate 2>/dev/null || true; echo prebuilt"
  },
  "deploy": {
    "preDeployCommand": "node ./scripts/runtime/railway-predeploy.cjs",
    "startCommand": "bash start.sh",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
