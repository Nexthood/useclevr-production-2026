{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "echo prebuilt"
  },
  "deploy": {
    "preDeployCommand": "corepack enable && corepack prepare pnpm@11.5.0 --activate && node ./scripts/runtime/railway-predeploy.cjs",
    "startCommand": "node -r ./scripts/runtime/load-env.cjs ./scripts/runtime/start-dist.cjs",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 300,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
