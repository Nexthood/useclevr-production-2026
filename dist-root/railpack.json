{
  "provider": "node",
  "steps": {
    "install": {
      "commands": ["echo 'dependencies pre-installed in standalone bundle'"]
    },
    "build": {
      "inputs": [{ "step": "install" }],
      "commands": ["echo prebuilt"]
    }
  }
}
