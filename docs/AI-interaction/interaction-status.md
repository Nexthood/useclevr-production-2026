# AI Interaction Status

Update this file after every completed AI interaction.

## Current Interaction

- **Date**: 2026-09-20
- **Goal**: Add a structured message step to the Usy contact handoff: after department selection Usy asks for the request in a dedicated Message input (10–500 characters, live 0/500 counter, client and server validation) before collecting name, reply email, and optional company, preserving department routing, the central n8n handoff, guest and authenticated modes, and all six supported languages. No commits or pushes.
- **Durable change**: `src/lib/usy/contact.ts` pins the 10–500 message rule into the contact payload schema, adds `validateContactMessage` plus localized staged prompts (department, message, message problems, contact details), and only stores messages passing the rule; `src/lib/usy/router.ts` runs the progressive department → message → details → confirmation flow with `messageInput` signaling, short-cancel support, and draft preservation across steps; `src/app/api/usy/chat/route.ts` bounds draft fields and prefills name/reply email from the verified server session for signed-in users; `src/components/ui/help-chatbox.tsx` renders the dedicated message composer with live counter, validation hint, and cancel; `scripts/ai/test-usy-contact-message-step.ts` (`test:usy-contact-message-step`, in `test:all`) pins the flow; `CHANGELOG.md` and `requirements.md` record the user-visible behavior.
- **Verification**: `test:usy-contact-message-step`, `test:usy-guest-mode`, `test:usy-billing-knowledge` (16), Usy secretariat, and full `test:all` pass; `tsc --noEmit` exit 0; ESLint 0 errors on changed files and repo-wide; secret-leak and pricing checks pass; Business stays €420/month with 1,500 AI credits and 100 datasets.
- **Residual risk**: a guest message starting with a cancel word within 20 characters is treated as a cancel command at the message step; combined one-shot inputs starting with contact-intent phrases route to the message step for retyping instead of being captured.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)

- **Date**: 2026-09-20
- **Goal**: Fix Investor dashboard semantic consistency without changing correct deterministic calculations or the Investor flow: a chart may be labeled a trend only with at least two distinct valid time periods, one-period data must render as a metric snapshot, a non-calculable metric must render an unavailable state, "Today's Score"/"AI confidence" must be renamed to their real semantics with short explanations, and the Business Balanced Scorecard must stay a distinct metric. No commits or pushes.
- **Durable change**: `src/lib/data/trend-semantics.ts` (new) defines the shared deterministic trend/snapshot/unavailable rules; `src/lib/data/dashboard-semantic-profile.ts` extends `DashboardSemanticTrend` with `state`/`snapshotValue`, adds `buildTrendPanel`, and covers every profile including a new `investor_portfolio` branch; `src/app/(auth)/app/page.tsx` renders trend panels by state, trusts the semantic profile over the page-level fallback, relabels the daily brief score to "Business Health Score" and its confidence to "Analysis confidence" with formula explanations, renames the overview composite cards to "Workspace Health Score"/"Analysis Confidence", and shows the BBSC methodology line on the scorecard preview; `src/app/(auth)/app/daily-health/page.tsx` reuses the shared labels; `src/lib/executive/daily-health-semantics.ts` (new) centralizes score labels/copy and `daily-health.ts` exports `calculateMetrics`/`buildDeterministicBrief` for tests; `test:trend-semantics` and `test:score-semantics` (new, in `test:all`) pin the ten trend cases and the score semantics; `test-investor-questions.ts` asserts state-aware investor semantics (revenue snapshot value 126384909.53, profit unavailable); CHANGELOG and requirements record the behavior.
- **Verification**: `test:trend-semantics`, `test:score-semantics`, `test:investor-questions`, `test:dashboard-semantic-profiles`, `test:investor-portfolio-aggregation`, `test:bbsc`, `test:report-accuracy`, `test:profitability-period-trend`, `test:profitability-two-file`, `test:profitability-report-entry-points`, `test:saas-semantic-profile`, `test:business-model-routing`, `test:business-semantics`, `test:dashboard-selected-dataset-routing` pass; `tsc --noEmit` exit 0; ESLint 0 errors on changed files; lint:secrets, lint:todos, lint:changelog, lint:package pass; Investor KPI and PDF values unchanged; `test:dataset-aware-report-profiles` fails at a saas findings-metadata assertion verified pre-existing at pristine HEAD via a temporary worktree.
- **Residual risk**: the no-selected-dataset dashboard view still uses page-level generic series heuristics (KPI sparklines can plot annual revenue over investment dates); report/PDF trend guards keep per-site ≥2 checks; the pre-existing dataset-aware report-profiles failure needs an owner outside this change.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)

- **Date**: 2026-09-20
- **Goal**: Fix `POST /api/usy/chat` returning 401 for anonymous visitors and implement Usy Guest Mode (public product/billing knowledge without any private-data exposure) alongside the preserved Authenticated Mode, plus fix the Free plan rendering `$0/€0` simultaneously instead of one market-resolved currency. No weakening of NextAuth or other protected APIs; no second Usy implementation; no commit/push.
- **Durable change**: `src/proxy.ts` allowlists `/api/usy/chat` and `/api/usy/contact` as exact public API paths (the 401 root cause: the Next.js 16 edge proxy rejected every non-allowlisted unauthenticated API call before the guest-capable handlers ran); `src/lib/usy/router.ts` derives roles only from the verified server session, adds `buildUsyRequestContext` (drops client-supplied plan/usage for guests), `isUsyContextAuthenticated`, a 6-language deterministic sign-in-required answer for guest personal-account questions, and `signInRequired` copy in all six languages; `src/app/api/usy/chat/route.ts` distinguishes guest/authenticated modes from the server session and passes the boundary-checked context; `src/components/ui/help-chatbox.tsx` maps 401/429/400/5xx to deterministic user-facing messages; `formatPlanPrice(plan, market)` in `src/lib/billing/plans.ts` resolves Free through the authoritative market resolver so Free renders one primary currency per market (US $0, EU €0, UK £0, CA CA$0) with the pricing page, checkout, subscription selector, and account center passing the resolved market.
- **Verification**: `test:usy-guest-mode` (new, wired into `test:all`) covers guest public answers, sign-in-required personal questions in 5 languages, forged-usage stripping, authenticated preservation, session-only role derivation, proxy allowlist guards, and the 30/min guest rate limit; `test:usy-billing-knowledge` 16/16, Usy secretariat, `test:pro-pricing` (extended per-market Free price assertions), `test:business-plan-limits`, `test:zero-credit-ux`, `test:tier-resolution`, `test:sidebar-credit-topup-link`, `test:hybrid-ai-gates`, and full `test:all` pass; `tsc --noEmit` exit 0; ESLint 0 errors; pricing/secret/package record checks pass. Business stays €420/month, 1,500 credits, 100 datasets; Stripe checkout unchanged.
- **Residual risk**: guest personal-data detection is deterministic keyword matching (unusual phrasings fall through to static knowledge answers that contain no private data, since the Usy path performs no user lookup); non-marker phrasings get the English sign-in answer via the existing language detector; `formatPlanPrice` without a market defaults to EU for downloads/admin tables.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)

- **Date**: 2026-09-19
- **Goal**: Implement a single authoritative credit-control system: one centralized feature-cost table (upload+standard analysis 10, AI Analyst message 1, report generation 3, forecast 3, profitability analysis 15, existing report download 0), all debits through the existing credit engine's reserve/finalize/release mechanism, one authoritative balance across all credit displays, zero-credit/insufficient behavior preserved, and legacy quota logic removed. No commits or pushes.
- **Durable change**: `src/lib/billing/feature-costs.ts` pins `FEATURE_CREDIT_COSTS` with flat deterministic costs and `CREDIT_VALUE_EUR = 0.085`; the credit engine remaps legacy action names, removes the dead token-priced debit path, and makes finalize replay idempotent (`type: "charge"` lookup); uploads (server action, datasets API, upload/simple API) and dataset re-analysis debit the unified 10-credit `standard_upload_analysis`; `/api/analyze` debits 1 credit per AI Analyst message and captures Gemini token metadata internally; the chat analytical branch is no longer a credit bypass; report routes debit 3 through the registry while report downloads stay 0; `useUsage`/`UsageMonitor`, the Downloads page, subscription, profile, and account-center displays all derive the primary number from the server `availableCredits`; legacy systems removed (`/api/usage/increment`, `consumeAnalystCredit`, `credits-context.tsx`, unused report button, duplicate cost/package constants, duplicate plan-sync that could reset purchased credits); `scripts/billing/test-unified-credit-control.ts` adds 12 behavioral checks (master-prompt cases A–K) wired into `test:all`.
- **Verification**: `test:all` passes (including the new `test:credit-unified` 12/12, `test:zero-credit-ux`, `test:credit-engine`, `test:credit-lifecycle-downgrade`, `test:credit-topup-packages`, `test:sidebar-credit-topup-link`); `pnpm exec tsc --noEmit` exit 0; ESLint 0 errors; `pnpm lint:changelog` passes.
- **Residual risk**: the forecast cost (3) is registered but no standalone endpoint debits it yet; the accountancy/pre-bookkeeping flows remain credit-exempt by entitlement design; `processPlanChange` can leave `included + purchased ≠ remaining` when purchased exceeds the new plan allowance, so every display uses `remaining − reserved` as the authoritative number.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)

- **Date**: 2026-09-19
- **Goal**: Restore the public /subscription route that returns 404 on app.useclevr.com, reusing the existing pricing configuration and components without touching the authenticated billing flow, the landing page, or the credit lifecycle.
- **Durable change**: `src/app/(public)/subscription/page.tsx` (new) renders the existing `PublicPricingPlans` component in the public route group, so plan names, prices, market pills, the monthly/yearly toggle, and the signup/checkout CTAs all come from the authoritative `billingPlans` and `launch-pricing` source with no duplicated pricing logic.
- **Verification**: full history sweep across all branches, dist build output, and proxy/config history found no public /subscription page in this 2026 repository (root cause: migration loss); `pnpm exec tsc --noEmit` is clean for the new file; dev smoke test shows GET /subscription returns 200 with no redirect for a logged-out visitor, rendering Free €0, Pro €40/month, Business €420/month, the Most Popular badge, the Monthly/Yearly toggle, and CTAs to /signup and /app/settings/checkout with plan and interval parameters.
- **Residual risk**: `pnpm build` currently fails only on other agents' in-flight files (credit-engine test script, upload/simple, analyze route, usy router) and must be revalidated with the whole tree before deploy.
- **Date**: 2026-09-19
- **Goal**: Final integration check of the combined working tree from the four completed parallel billing/credit tasks (master lifecycle, zero-credit UX, Usy billing knowledge, Free preserved-purchased production regression) before the normal `beta` commit/push.
- **Durable change**: no functional change; verification only. The combined tree keeps one authoritative business model across the shared credit engine, 402 creditState payloads, the zero-credit modal, the sidebar usage monitor, and Usy billing knowledge.
- **Verification**: `test:credit-lifecycle-downgrade` 10/10 (including Free + included 0 + purchased 100 → standard upload succeeds and purchased balance decreases), `test:zero-credit-ux` 12/12, `test:credit-topup-architecture` 18/18, `test:credit-topup-packages` 19/19, `test:credit-engine` 17/17, `test:usy-billing-knowledge` 10/10, `test:upload-credit-reservation` 8/8, `test:sidebar-credit-topup-link` and `test:tier-resolution` pass; `tsc --noEmit` exit 0; ESLint 0 errors on all combined changed files.
- **Residual risk**: the production reports persist until this working tree deploys; the parallel agent's in-progress `src/lib/usy/*` files must compile before the shared branch validates end to end.
- **Date**: 2026-09-19
- **Goal**: Land the Pro-refund recovery end to end and align Usy with the same authoritative billing rules: subscription canceled → existing lifecycle sync downgrades to Free, Pro included credits reset, purchased credits preserved and usable on Free, top-up purchase gated to Pro/Business, and Usy explains exactly these rules (master prompt section 13).
- **Durable change**: `src/app/api/checkout/credit-topup/route.ts` gates top-up purchase to Pro/Business; `src/lib/billing/credit-engine.ts` keeps tier-agnostic reservation with included-before-purchased consumption so preserved purchased credits stay usable on Free; `src/app/(auth)/app/settings/subscription/page.tsx` adds the Subscription Invoices section with refund states; `src/lib/usy/billing-knowledge.ts` (new) plus `src/lib/usy/router.ts` intents (top-ups, refunds, downgrade, cancellation) give Usy deterministic, localized billing answers; `scripts/billing/test-credit-lifecycle-downgrade.ts` (10 cases), `scripts/ai/test-usy-billing-knowledge.ts` (10 cases, `test:usy-billing-knowledge`), and `scripts/billing/test-zero-credit-ux.ts` pin the contracts; `CHANGELOG.md` records the user-facing changes.
- **Verification**: production state verified read-only after the recovery — Stripe subscription canceled; profile tier Free / status canceled; UserCredit included 0, purchased 100, remaining 100; top-up history 2 Refunded + 1 Completed; PLAN_RESET ledger recorded. Suites: `test:credit-lifecycle-downgrade` 10/10, `test:zero-credit-ux` 12/12, `test:usy-billing-knowledge` 10/10, `test:credit-topup-packages` 19/19, `test:credit-topup-webhooks` 68, `test:credit-topup-architecture` 18, `test:upload-credit-reservation` 8/8, `test:sidebar-credit-topup-link` pass, Usy secretariat pass; `tsc --noEmit` exit 0; ESLint clean on touched files; changelog/records/todos checks pass.
- **Residual risk**: the production fixes require the next deploy of the branch; refund recovery of the two top-ups already completed earlier in this session; the confirmation email keeps the payment reference per its existing contract.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)

## Previous Interaction

- **Date**: 2026-09-19
- **Goal**: Show a clear out-of-credits modal instead of a generic error whenever an AI/credit-consuming action is rejected: Pro/Business get Add Credits linking to the billing credit-topups section, Free gets Upgrade to Pro or Business because Free cannot purchase top-ups, the modal uses the authoritative usable balance, the UI never bypasses server-side enforcement, and concurrent requests stay safe with non-negative balances.
- **Durable change**: `src/lib/billing/credit-exhaustion.ts` derives an authoritative `creditState` payload (zero-clamped usable balance, exhaustion reason, plan-aware CTA and copy) from the credit account or a usage snapshot; the analyze, chat, report-generation, and both upload APIs embed `creditState` in every 402 credit-exhaustion response; `src/components/shared/zero-credit-modal.tsx` adds a display-only modal plus `useZeroCreditModal` 402 hook; the chat panels, CSV upload, and retail inventory analysis route exhausted responses into the modal.
- **Verification**: `test:zero-credit-ux` and the billing suites pass; `tsc --noEmit` clean for all touched files; ESLint 0 errors.
- **Residual risk**: `/api/mcp` and the upload spending-limit 402s still return plain messages without `creditState` because spending limits are a separate exhaustion reason.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)
