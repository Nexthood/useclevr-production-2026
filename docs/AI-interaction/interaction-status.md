# AI Interaction Status

Update this file after every completed AI interaction.

## Current Interaction

- **Date**: 2026-09-16
- **Goal**: Trace both failing Profitability report-generation entry points end to end, confirm whether they share one backend, fix the exact HTTP 500 root cause without touching working upload, Profitability calculations, dataset persistence, credits, dashboard analytics, ClevrSync, Stripe, or subscription logic, and add regression coverage for both buttons.
- **Durable change**: Both report buttons share `POST /api/reports`; the HTTP 500 came from `checkActionEnforcement` → `getConcurrentAnalysisCount` querying the never-migrated `ConcurrentAnalysisCount` relation. The enforcement lookup and its increment/decrement helpers now fail open with debug logs like `getDailyRequestCount`; the Railway predeploy registers a new `0033_concurrent_analysis_count.sql` migration creating the missing table and its per-user unique index; `buildProfitabilityReportInput` embeds report diagnostics so persisted Profitability reports satisfy the current runtime and same-key retries replay the stored report without duplicate charges; `POST /api/reports` returns structured failures (`DATASET_ROW_COUNT_MISMATCH` with HTTP 422 for integrity failures, `REPORT_GENERATION_FAILED` with HTTP 500 otherwise) and no longer masks the response when credit release throws; the Profitability Analysis page toast shows the structured error message. Retired as `.TODO` task T-1059.
- **Verification**: `pnpm test:profitability-report-entry-points` (both entry-point payloads against a canonical paired 8,500-row Profitability analysis with the exact production `relation "ConcurrentAnalysisCount" does not exist` failure installed) passes; `pnpm test:profitability-two-file`, `pnpm test:dataset-aware-report-profiles`, and `scripts/billing/test-upload-credit-reservation.ts` pass; `pnpm exec tsc --noEmit` exits clean; `git diff --check` passes. Live verification with the canonical dataset owner reproduced both 500s before the fix and, after the fix, both buttons return HTTP 200, both reports register under Reports & Downloads with downloadable PDFs, the same-key retry replays the stored report idempotently, and each generation charges exactly one report_generation reservation (497 → 457 credits for the two distinct reports, no charge on replay).
- **Residual risk**: Production convergence requires the next Railway deploy whose predeploy applies 0033; until then the fail-open enforcement keeps report generation working while the concurrent-analysis limit reads as zero. `scripts/analysis/test-full-row-report-semantic-consistency.ts` fails in this local environment at XLSX fixture creation (`xlsx` ESM namespace import under tsx cannot write files) — reproduced with no changed files involved, pre-existing and unrelated to this fix.
- **Detailed record**: [Interactive log](../../project-logs/interactive-log.md)
- **Activity summary**: [Activity log](../../project-logs/activity-log.md)
