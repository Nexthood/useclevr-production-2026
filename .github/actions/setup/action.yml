name: Setup Node and pnpm
description: Setup Node, activate Corepack pnpm, install dependencies

inputs:
  node-version:
    description: Node.js version
    required: false
    default: "26.x"

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v6
      with:
        node-version: ${{ inputs.node-version }}
        cache: pnpm

    - name: Activate pnpm through Corepack
      shell: bash
      run: |
        corepack enable
        corepack prepare pnpm@11.5.0 --activate

    - name: Add pnpm to PATH and install dependencies
      shell: bash
      run: |
        PNPM_HOME="$(corepack env)"
        echo "Corepack pnpm location: $PNPM_HOME"
        echo "$PNPM_HOME" >> "$GITHUB_PATH"
        export PATH="$PNPM_HOME:$PATH"
        pnpm install --frozen-lockfile
